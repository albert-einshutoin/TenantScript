---
"@tenantscript/cli": minor
---

Add the built-in `webhook-transformer` template to `ext init`, including least-privilege defaults,
behavior tests, security guidance, and fail-closed option validation.
