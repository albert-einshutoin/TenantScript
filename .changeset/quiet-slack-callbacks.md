---
"@tenantscript/control-plane": minor
---

Add a browser-safe Slack OAuth callback HTTP route with fixed redirects, explicit binding-cookie deletion, app-scoped encrypted secret references, real Worker storage composition, and replay-safe state handling.
