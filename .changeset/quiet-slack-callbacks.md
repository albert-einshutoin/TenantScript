---
"@tenantscript/control-plane": minor
---

Add a browser-safe Slack OAuth callback HTTP route with fixed redirects, binding-cookie deletion across configuration failures, app-scoped encrypted secret references, release-policy protection, real Worker storage composition, and replay-safe state handling.
